# Скопируй в .env и при необходимости измени.
# Локально: postgres на localhost или Docker (порт 5432).
# Render/продакшен: DATABASE_URL задаётся в панели сервиса.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/taskpulse?schema=public"
